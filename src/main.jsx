import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.jsx";
import "./server.js"; // start MirageJS server

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
